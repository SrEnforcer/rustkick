# rustkick
Experimental project playing with a hard kick generator MIDI in Rust

## Run standalone locally

Use the shortcut command:

```bash
cargo dev
```

This is equivalent to:

```bash
PULSE_SERVER=unix:/mnt/wslg/PulseServer cargo run --bin hardkick-standalone --release
```
